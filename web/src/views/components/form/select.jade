select(ng-switch-when='Select', required='{{field.required}}',
       id='form-{{section.form_template_id}}-{{$index}}')
  option(ng-repeat='opt in field.opts', value='{{$index}}',
         ng-bind='opt') 

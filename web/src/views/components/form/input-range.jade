span.form-slider(ng-switch-when='Slider')
  input(ng-switch-when='Slider', type='range', 
        ng-model='field.submit_input', 
        min='{{field.slider_floor}}',
        max='{{field.slider_ceiling}}',
        step='{{field.slider_step}}')
